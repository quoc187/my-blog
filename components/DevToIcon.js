import Icon from "./Icon";
// import devIcon from '../assets/images/dev-to.svg'
import { cls } from "../utils/template";

const DevToIcon = ({ className, size }) => {
  return <Icon src={""} size={size} className={className} />
}

export default DevToIcon;